<% if user.already_followed?(current_user) %>
  <%= link_to  "取消追蹤", user_follows_path(user), method: :delete, remote: true %>
<% else %>
  <%= link_to  "追蹤", user_follows_path(user), method: :post, remote: true %>
<% end %>
