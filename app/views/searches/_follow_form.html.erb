<%= form_with(url: search_follow_searches_path(user_id: user), method: :get, class: "search-form") do |f| %>
  <%= f.text_field :keyword , placeholder: "搜尋", autocomplete: "off"%>
  <%= f.text_field :user_id, type: "hidden", value: "#{user.id}"%>
<% end %>
